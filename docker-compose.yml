services:

  backend:
    build:
      context: ./backend/CiCdDemo.Api
    ports:
      - "5000:8080"

  frontend:
    build:
      context: ./frontend
    ports:
      - "3000:3000"
    depends_on:
      - backend